# Changelog

All notable changes to this project are documented here.

---

## [Unreleased]

---

## 2026-05-27

### Fixed
- **Preserve selection on panel collapse** — the Segmentation panel body is now kept mounted (hidden via `display: none`) instead of unmounted, so channel filter and segment selections survive collapse/expand cycles.
- **Reset "Show All" on dropdown close** — the expanded channel chip list now collapses back to the default view whenever the dropdown drawer is closed, not only when the search query changes.
- **"All" segment row visibility** — the "All" row in the segment list now appears whenever more than 2 segments are visible, regardless of whether a channel filter is active. Previously it was hidden when filtering narrowed the list.

### Added
- `.gitignore` — covers `node_modules/`, build output (`dist/`), `.env` secrets, editor/OS noise, and TypeScript build info.
- `CLAUDE.md` — project context file for Claude Code.

### Chore
- Fixed git remote configuration: renamed `origin` → `figma-make` (read-only Figma Make source) and `dev` → `origin` (the `-dev` working repo), so `git push` always targets the correct repository.

---

## 2026-05-20

### Added
- **GitHub Pages deployment** via GitHub Actions (`.github/workflows/`). Builds with pnpm and deploys to `https://jonasaschmidt-cm.github.io/Nativepersonalizationvariantsegmentselectorchannelfilter-dev/` on every push to `main`.

### Changed
- **Search UX and visual polish** — search input styling, placeholder text, result highlighting improvements.
- **Highlight and chip polish** — match highlighting in segment names and channel chips; chip border/color refinements.
- **UI polish** — separator, chip styles, highlight, and layout tweaks across the segment selector.

### Removed
- Unused `TagFilterVariant2`, `TagFilterVariant3`, and `TagFilterVariant4` components (Figma Make generated alternatives, not used in this prototype).

---

## 2026-05-20 — Initial import from Figma Make

Initial code bundle generated by Figma Make from the Figma file  
[Native Personalization — Variant — Filter Segments/Profiles by Channels](https://www.figma.com/design/eGUd1gIAQgq5cehwx8iIYX/Native-Personalization---Variant---Filter-Segments-Profiles-by-Channels).

Includes the full React + Vite + Tailwind scaffold, shadcn/ui component library, and the initial `TagFilterVariant1` segment selector component.
